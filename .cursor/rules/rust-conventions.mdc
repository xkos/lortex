---
description: Rust 编码规范 — 模块组织、测试、命名等约定
globs: rust/**/*.rs
alwaysApply: false
---

# Rust 编码规范

## 模块组织（Rust 2018+ 风格）

使用 `模块名.rs` 作为模块根文件，子模块放在同名目录下。**不使用 `mod.rs`**。

```
✅ 正确
src/
├── lib.rs
├── model.rs            ← model 模块根
├── model/
│   ├── object.rs
│   └── term.rs
├── sqlite.rs           ← sqlite 模块根
└── sqlite/
    ├── object.rs
    └── term.rs

❌ 错误
src/
├── lib.rs
├── model/
│   ├── mod.rs          ← 不要用 mod.rs
│   ├── object.rs
│   └── term.rs
```

注意：`model.rs` 和 `model/mod.rs` 不能同时存在，Rust 编译器会报 E0761 错误。

## 单元测试

测试写在同一个文件底部，用 `#[cfg(test)]` 隔离：

```rust
pub fn add(a: i32, b: i32) -> i32 {
    a + b
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_add() {
        assert_eq!(add(1, 2), 3);
    }
}
```

需要初始化环境的测试（如数据库），在测试模块中写 helper 函数：

```rust
#[cfg(test)]
mod tests {
    use super::*;

    fn setup() -> SqliteStore {
        SqliteStore::open_in_memory().unwrap()
    }

    #[test]
    fn test_create_object() {
        let store = setup();
        // ...
    }
}
```
