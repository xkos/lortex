---
description: ai2ai 文档维护规则 — 定义 AI 在每次迭代中如何维护项目状态文档
alwaysApply: true
---

# ai2ai 文档维护规则

## 什么是 ai2ai

`docs/ai2ai/` 是 AI 自维护的项目状态文档，与人维护的 me2ai 文档（prds/、tech/、AGENTS.md、rules/）互补。

- me2ai = "应该是什么样"（需求、架构、规范）
- ai2ai = "现在实际是什么样"（状态、进度、微决策）

## 文件结构

```
docs/ai2ai/
├── status.md              — 全局状态快照（覆盖更新）
├── checklist.md           — 用户验收测试清单（AI 生成/维护，人审核/勾选）
├── test-suite.md          — 全局用例库（从 checklist 沉淀，长期回归）
├── decisions.md           — 技术微决策 + 踩坑记录（只追加）
├── tasks/                 — 迭代任务分解（事前计划）
│   └── NNN-xxx.md
└── iterations/            — 迭代归档（事后总结）
    └── NNN-xxx.md
```

tasks/ 和 iterations/ 共享编号，一一配对。

## 迭代工作流

### 0. 需求澄清（迭代开始前，按需）

收到实现需求后，先判断是否需要澄清：

**需要澄清的情况**（不跳过）：
- 需求描述模糊或开放，存在多种理解
- 涉及多种可行方案需要选择
- 需求范围可能超出一次迭代的合理规模

**可跳过的情况**：
- 用户给出了详细实现方案和步骤
- 明确的 bug 修复，现象和预期都清晰
- task 文件已存在，只是继续执行

澄清流程：
1. 对需求中模糊、矛盾、遗漏的部分提出具体问题
2. 明确"做什么"和"不做什么"的边界（YAGNI 原则）
3. 如果需求复杂，分 2-3 段分别确认
4. 输出设计摘要："将要做什么、怎么做、不做什么"，等用户确认后再进入步骤 1

### 1. 迭代开始前：创建分支 + 生成 task 文件

**创建特性分支**：从主分支创建 `iter/NNN-xxx` 分支，所有变更提交到此分支。

创建 `docs/ai2ai/tasks/NNN-xxx.md`，内容包括：

```markdown
# 任务 NNN: [标题]

> 状态：🔨 进行中
> 分支：iter/NNN-xxx
> 配对迭代：[iterations/NNN-xxx.md](../iterations/NNN-xxx.md)

## 迭代目标
[一句话描述本次迭代要达成什么]

## 验收标准（人审核/补充）
[从用户视角描述"什么算成功"，AI 初始生成，人审核时可增删改]
- 能做到 XX
- 能看到 YY
- ZZ 功能不受影响

## 任务分解
- [ ] T1: [任务描述]
  - 验证：[具体的验证标准]
- [ ] T2: ...
```

任务分解原则：
- 每个任务是独立可实现的原子单元
- 每个任务有明确的验证标准（不是"实现 XX"，而是"XX 能做到 YY"）
- 任务按依赖顺序排列
- 单次迭代任务数建议 3-8 个，超过则拆分迭代
- **YAGNI**：只规划当前迭代需要的功能，不提前预设未来需求

### 2. 迭代进行中：TDD + 自审

#### 测试驱动开发（service / store / infra 层）

采用**红-绿-重构**循环：

1. **红** — 先写一个描述预期行为的失败测试
2. **绿** — 写**最小**的实现代码让测试通过（不多不少）
3. **重构** — 在测试保护下优化代码结构，确保测试仍然通过
4. **提交** — 每次绿→重构完成后立即提交

约束：
- 只实现测试要求的行为，不提前为未来需求写代码
- 如果发现先写了实现再补测试，应停下来，删掉实现，从测试开始重来
- model 层的序列化/反序列化测试不强制 TDD，但必须有测试覆盖
- UI 层不要求 TDD，由用户验收 checklist 覆盖

#### AI 自审（每个 task 完成后）

每完成一个 task，在标记 `[x]` 之前执行自审：

1. **规格合规** — 实现是否符合 task 中定义的验收标准？有无偏离或遗漏？
2. **代码质量** — 有无明显的重复代码、过度抽象、命名不当？
3. **测试覆盖** — 新增的公开 API 是否都有测试？是否覆盖了异常路径？
4. **回归影响** — 改动是否可能影响已有功能？对应的回归测试是否存在？

发现问题立即修复，修复后再标记 task 完成。自审结果不需要单独文档。

#### 更新 task 状态

- 自审通过后将 `[ ]` 改为 `[x]`
- 如果任务需要调整（拆分、合并、放弃），直接在 task 文件中修改并标注原因

### 2.5 子代理驱动开发（可选）

当迭代包含 3 个以上**互相独立**的任务时，可以使用子代理并行执行：

- 每个子代理处理一个独立任务，携带完整上下文（task 描述、相关文件、验证标准）
- 主代理负责分发任务、收集结果、执行两轮 review：
  - 第一轮：**规格合规** — 实现是否符合 task 要求
  - 第二轮：**代码质量** — 代码是否清晰、测试是否充分
- 不合格的任务退回重做，不是打补丁

不适用场景：任务有顺序依赖、涉及同一模块/文件的修改。

### 3. 迭代结束后：归档

1. **更新 checklist.md**（详见下方"测试 Checklist 维护规则"）
2. **task 文件**：状态改为 `✅ 已关闭`
3. **新建 iteration 文件** `docs/ai2ai/iterations/NNN-xxx.md`：
   - 记录：目标、完成内容、未完成/遗留、回归影响
   - "测试结果"部分从 checklist.md 的勾选结果归档
4. **更新 status.md**：
   - 更新"最后更新"日期
   - 更新"当前迭代"指向
   - 更新模块就绪度表格
   - 更新端到端联通状态
   - 更新已知问题/技术债
5. **按需更新 decisions.md**

---

## 测试 Checklist 维护规则

### 文件

`docs/ai2ai/checklist.md` — 唯一的、活的用户验收测试清单。

### 职责分工

| 环节 | 谁做 | 做什么 |
|------|------|--------|
| 生成 checklist | AI | 迭代结束时，根据 task 的验收标准生成新测试项 + 继承回归项 |
| 审核 checklist | 人 | 确认测试项是否覆盖关心的验收点，可增删改 |
| 执行测试 | 人 | 逐项测试，通过打 `[x]`，失败标记 `❌` 并写备注 |
| 当轮修复 | AI | 本迭代验收标准内的失败项，AI 直接修复后人重新测试 |
| 归档 checklist | AI | 下一迭代开始时，将上次勾选结果快照到 iteration 归档文件 |
| 处理遗留失败项 | AI | 超出本迭代范围的失败项，纳入下一迭代的修复任务 |
| 沉淀全局用例 | AI | 归档时将具有长期价值的测试项提炼到全局用例库 |

### checklist.md 格式

```markdown
# 测试 Checklist

> AI 生成和维护，人审核和勾选。
> 当前迭代：NNN-xxx

## 本迭代新增
- [ ] [测试项描述]：[操作步骤] → [预期结果]
- [ ] ...

## 回归测试
- [ ] [测试项描述]：[操作步骤] → [预期结果]
- [ ] ...
```

### 生成规则

AI 生成 checklist 时：
- **本迭代新增**：从 task 文件的"验收标准"展开为具体的操作步骤 + 预期结果
- **回归测试**：从上一迭代的 checklist 中继承所有已通过的测试项（保持为回归项）
- 上一迭代失败的测试项：如果本迭代修复了，移到"本迭代新增"；如果未修复，保留在回归测试中并标注 `⚠️ 上次失败`
- 当功能被重构或移除时，同步删除对应的测试项

### 归档规则

当新迭代开始、AI 需要重置 checklist 时：
1. 将当前 checklist.md 的完整内容（含勾选状态）复制到对应的 `iterations/NNN-xxx.md` 的"测试结果"章节
2. 然后重置 checklist.md 为新迭代的内容

### 缺陷反馈环（失败项处理）

用户测试失败时的标记格式：

```markdown
- ❌ 导入目录：选择含 5 张 jpg 的目录 → 点击导入
  > 现象：弹出错误弹窗 "path not found"
  > 备注：目录路径含中文
```

失败项分两类处理：

| 类型 | 判断标准 | 处理方式 |
|------|---------|---------|
| 当轮修复 | 属于本迭代 task 验收标准范围内的缺陷 | AI 直接修复 → 人重新测试该项 → 通过后打 `[x]` |
| 下轮修复 | 超出本迭代范围（新发现的问题、体验优化等） | 标注 `→ 转入下一迭代`，AI 在下一迭代 task 中自动生成修复任务 |

当轮修复的交互流程：
1. 人在 checklist 中标记 `❌` + 写备注（现象、复现步骤）
2. 人将失败项发给 AI（可以直接选中 checklist 中的失败项）
3. AI 读取备注，定位问题，修复代码
4. AI 将该项状态改回 `[ ]`（待重新测试）
5. 人重新测试，通过则打 `[x]`

当轮修复受安全护栏约束：同一项连续修复 3 次仍未通过，转为下轮修复并报告。

### 全局用例库

`docs/ai2ai/test-suite.md` — 经过验证的、具有长期回归价值的测试用例。

与 checklist 的区别：
- checklist 是迭代级的，每次迭代重置
- test-suite 是项目级的，只增不减（除非功能移除）

#### 沉淀规则

AI 在归档 checklist 时，自动判断哪些测试项应沉淀为全局用例：
- 覆盖核心链路的端到端测试（如"导入 → 浏览 → 搜索"）
- 连续 3 次以上迭代都作为回归项通过的测试
- 曾经失败后修复的测试（踩过坑的地方更值得长期守护）

不沉淀的：
- 一次性的功能验证（如"骨架页面能显示"）
- 纯 UI 样式类测试

#### 继承规则

AI 生成 checklist 的回归测试时，来源变为两个：
1. 上一迭代 checklist 中通过的测试项（短期回归）
2. `test-suite.md` 中的全局用例（长期回归，每次迭代都包含）

如果两者有重复，去重保留一份。

## status.md 中的迭代指向

```markdown
## 当前迭代

- 上一迭代：[NNN-xxx](tasks/NNN-xxx.md) ✅ 已关闭
- 当前活跃：[NNN+1-yyy](tasks/NNN+1-yyy.md) 🔨 进行中（M/N 任务完成）
```

## 什么不算迭代

- 纯文档更新、纯讨论、纯 bug 修复（除非修复涉及架构变更）不需要新建 task/iteration 文件
- 但如果 bug 修复改变了模块状态，需要更新 status.md

## AI 自动化测试规则

AI 自动化测试与用户验收 checklist 互补，构成完整的测试体系：

| 测试类型 | 谁写 | 谁执行 | 覆盖范围 | 方法 |
|---------|------|--------|---------|------|
| 单元测试 / 集成测试 | AI | CI / AI | service、store、infra 层的逻辑正确性 | TDD 红-绿-重构 |
| 用户验收测试（checklist） | AI 生成，人审核 | 人 | 端到端功能、交互体验、回归保护 | 手动验收 |

### 编写要求（TDD）

**service / store / infra 层**采用 TDD 红-绿-重构：
1. 先写失败测试（描述预期行为）
2. 写最小实现让测试通过（YAGNI — 不多写）
3. 重构代码，确保测试仍通过
4. 提交

覆盖要求：
- **service / store 层**：每个公开函数必须有对应的单元测试，覆盖正常路径 + 至少一个异常路径
- **infra 层**：涉及文件 I/O、外部调用的函数需有集成测试（使用 tempdir 等隔离机制）
- **model 层**：结构体的序列化/反序列化需有测试（不强制 TDD，但必须覆盖）
- **UI / TUI 层**：不要求自动化测试，由用户验收 checklist 覆盖

### 运行时机

- **实现过程中**：每完成一个 task，运行 `cargo test` / `flutter test`，确认不破坏已有功能
- **迭代结束时（门禁）**：`cargo test` 和 `flutter test` 全量通过后，才进入 checklist 生成环节
- 测试不通过时：修复后重新运行，不得跳过

### task 文件中的测试要求

task 文件的任务分解中，测试相关的验证标准应明确标注：

```markdown
- [ ] T3: 实现 DocumentStore
  - 验证：TDD — 先写 load / save / delete 的失败测试，再实现，含文件不存在的异常路径
  - 验证：`cargo test` 全量通过
```

## 安全护栏

- **错误循环检测**：连续 3 次尝试相同修复未成功时，停止尝试，报告问题现象、已尝试的方案、可能的原因，等待人工介入
- **变更范围感知**：单次迭代修改文件超过 10 个时，暂停并确认是否需要拆分迭代

## 模块状态标记

| 标记 | 含义 |
|------|------|
| ✅ 可用 | 功能已实现且可正常使用 |
| 🔨 部分 | 部分功能已实现，或骨架已搭建 |
| 🔨 骨架 | 仅有接口/结构定义，无实际逻辑 |
| ❌ 空壳 | 文件存在但无实现 |
| ❌ 未开始 | 尚未创建 |

## 经验库记录类型

`docs/ai2ai/decisions.md` 中使用三种前缀：

| 前缀 | 类型 | 记录什么 |
|------|------|---------|
| D | 技术微决策 | 设计文档未覆盖的实现选型 |
| L | 踩坑记录 | 技术限制、意外行为、绕过方案 |
| B | Boundary 变更 | AI 决策边界规则的新增/调整 |

## 测试项书写规范

无论是 checklist 还是 iteration 中的测试项，每项必须包含：
- 具体的操作步骤（用户做什么）
- 预期结果（应该看到什么）
- 如果涉及已有功能，标注是否为回归测试

示例：
```
| # | 测试项 | 操作步骤 | 预期结果 |
|---|--------|---------|---------|
| 1 | 目录扫描 | 在 Import 面板选择一个包含 5 张图片的目录 | 列表显示 5 个文件，含文件名和大小 |
| 2 | [回归] App 启动 | flutter run -d macos | 窗口正常显示，无崩溃 |
```
